// colors.js
export const getColors = (theme) => {
  return {
    BG_COLOR: theme === "dark" ? "#1E1E1E" : "#FFFFFF",
    TEXT_COLOR: theme === "dark" ? "#FFFFFF" : "#1E1E1E",
  };
};

// 1E1E1E FFFFFF
